<% if object.errors.any? %>

<div class="errors">
	<p><%= pluralize(object.errors.count, "error") %>
	<ul class="errorList">
	<% object.errors.full_messages.each do |message| %>
		<li><%= message %></li>
	<% end %>
	</ul>
</div>

<% end %>